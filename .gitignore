# PII-bearing build outputs and source exports — never commit these.
# docs/ (the PWA shell) is the only thing meant to be pushed to GitHub Pages.
PVH_Field_App.html
PVH_data.json
*.xlsx

# Operations note — records where the data lives and how access works.
# Kept outside the published repo on purpose.
PVH_operations_note.md

# Local Claude Code settings
.claude/

# Python
__pycache__/

# Daemon runtime artifacts -- local scheduling state and build logs.
daemon.lock
logs/

# Machine-specific daemon settings (SSRS host, report folder, Dropbox path).
# Commit pvh_local_config.example.json instead.
pvh_local_config.json
